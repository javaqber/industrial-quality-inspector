# IQI Aluminium — Python dependencies (Fase 0B)
# Only production dependencies for the Claude Vision + FastAPI + PostgreSQL stack.
# YOLO/ultralytics removed — see scripts/legacy_yolo/ for the original YOLO app.

fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
pydantic==2.9.0
anthropic>=0.40.0
httpx>=0.27.0
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.1
